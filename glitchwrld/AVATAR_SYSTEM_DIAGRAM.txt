╔══════════════════════════════════════════════════════════════════════════════╗
║                    AVATAR GAME MODE SYSTEM - ARCHITECTURE                    ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                           USER INPUT LAYER                                   │
└──────────────────────────────────────────────────────────────────────────────┘

    Keyboard                 Mouse                    UI Panel
    ┌──────┐                ┌──────┐                 ┌──────┐
    │ ↑ ↓  │                │      │                 │ Leva │
    │ ← →  │                │ Move │                 │Panel │
    │ WASD │                │      │                 │      │
    │Space │                └──────┘                 └──────┘
    │Shift │                    │                        │
    │ G C  │                    │                        │
    └──────┘                    │                        │
        │                       │                        │
        └───────────────────────┴────────────────────────┘
                                │
                                ▼

┌──────────────────────────────────────────────────────────────────────────────┐
│                         INPUT PROCESSING LAYER                               │
└──────────────────────────────────────────────────────────────────────────────┘

            ┌────────────────────────────────────────┐
            │    useAvatarMovement Hook              │
            │                                        │
            │  • Listens to keydown/keyup            │
            │  • Updates inputState in store         │
            │  • Runs every frame (useFrame)         │
            │  • Calculates velocity from input      │
            │  • Applies physics (gravity, jump)     │
            │  • Updates position in store           │
            └────────────────────────────────────────┘
                            │
                            ▼

┌──────────────────────────────────────────────────────────────────────────────┐
│                          STATE MANAGEMENT LAYER                              │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────────────────┐
    │                        avatarStore (Zustand)                         │
    ├──────────────────────────────────────────────────────────────────────┤
    │                                                                      │
    │  Game Mode:                     Movement State:                     │
    │  • gameModeEnabled              • position: Vector3(x, y, z)        │
    │  • cameraMode                   • velocity: Vector3(dx, dy, dz)     │
    │                                 • rotation: Euler(x, y, z)          │
    │  Input State:                   • isMoving, isJumping, isFalling    │
    │  • forward, backward            • animationState                    │
    │  • left, right                                                      │
    │  • jump, sprint                 Configuration:                      │
    │                                 • moveSpeed: 0.15                   │
    │  Camera Settings:               • sprintSpeed: 0.3                  │
    │  • cameraDistance: 8            • jumpForce: 0.5                    │
    │  • cameraHeight: 4              • gravity: -0.02                    │
    │  • cameraSmoothness: 0.1                                            │
    │                                                                      │
    └──────────────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                ▼                       ▼

┌─────────────────────────┐    ┌──────────────────────────────────┐
│   Camera Follow Hook    │    │      Render Layer                │
│                         │    │                                  │
│  useCameraFollow        │    │  <AvatarController>              │
│                         │    │    └─> <Avatar>                  │
│  • Reads position       │    │          ├─ Body (Cylinder)      │
│  • Calculates ideal     │    │          ├─ Head (Sphere)        │
│    camera position      │    │          ├─ Visor (Sphere)       │
│  • Lerps smoothly       │    │          └─ Point Light          │
│  • Updates camera.pos   │    │                                  │
│  • Disables OrbitCtrls  │    │  Position: avatarStore.position  │
│                         │    │  Rotation: avatarStore.rotation  │
└─────────────────────────┘    └──────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                          CAMERA MODES DIAGRAM                                │
└──────────────────────────────────────────────────────────────────────────────┘

    ORBIT MODE (Default)              THIRD-PERSON MODE              FIRST-PERSON MODE
    ────────────────────              ─────────────────              ─────────────────

         Camera                          Camera                        Camera
           👁                              👁                             👁
            \                              |                              |
             \                             |                              |
              \                         height=4                      eyeHeight=1.7
               \                           |                              |
                \                     ┌────┴────┐                    ┌────┴────┐
            OrbitControls          distance=8  |                    |    👨   |  (avatar)
                 \                      |   👨  |  (avatar)          └─────────┘
                  \                     └───────┘                    Look direction →
                   \                    Behind avatar
                    *                   Follows smoothly
                  Scene
                  Center           Keyboard: Arrow keys           Keyboard: Arrow keys
                                   Mouse: Not used                Mouse: Look around (future)


┌──────────────────────────────────────────────────────────────────────────────┐
│                         MOVEMENT PHYSICS FLOW                                │
└──────────────────────────────────────────────────────────────────────────────┘

    Every Frame (useFrame):

    1. READ INPUT STATE
       ┌────────────────────┐
       │ forward: true      │
       │ backward: false    │
       │ left: false        │
       │ right: true        │◄─── From avatarStore.inputState
       │ jump: false        │
       │ sprint: false      │
       └────────────────────┘
              │
              ▼
    2. CALCULATE DIRECTION
       ┌────────────────────┐
       │ moveDir.z = -1     │  (forward)
       │ rotateDir = -0.05  │  (turn right)
       └────────────────────┘
              │
              ▼
    3. APPLY ROTATION
       ┌────────────────────┐
       │ rotation.y -= 0.05 │
       │ rotatedDir =       │
       │   moveDir.applyEuler(rotation)
       └────────────────────┘
              │
              ▼
    4. CALCULATE VELOCITY
       ┌────────────────────┐
       │ speed = moveSpeed  │  (or sprintSpeed if Shift)
       │ vx = dir.x * speed │
       │ vz = dir.z * speed │
       │ vy += gravity      │  (if in air)
       └────────────────────┘
              │
              ▼
    5. UPDATE POSITION
       ┌────────────────────┐
       │ position += velocity│
       │                    │
       │ if (y <= 2) {      │  (ground collision)
       │   y = 2            │
       │   grounded = true  │
       │ }                  │
       └────────────────────┘
              │
              ▼
    6. UPDATE ANIMATION STATE
       ┌────────────────────┐
       │ if (grounded) {    │
       │   if (speed > 0.2) │
       │     state = "run"  │
       │   else if (speed>0)│
       │     state = "walk" │
       │   else             │
       │     state = "idle" │
       │ } else {           │
       │   state = "jump"   │
       │ }                  │
       └────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                        INTEGRATION WITH EXISTING SCENE                       │
└──────────────────────────────────────────────────────────────────────────────┘

    HybridScene.tsx
    ├── Canvas (R3F)
    │   └── SceneContent
    │       ├── Lighting
    │       ├── MultiLayerStarfield
    │       ├── ConstellationLayer
    │       ├── GalaxyNebulaClouds
    │       ├── AccurateSolarSystem
    │       ├── <AvatarController /> ◄──────── NEW (ADD THIS)
    │       ├── OrbitControls (disabled when game mode active)
    │       └── EffectComposer (Bloom, Noise, Vignette)
    │
    └── UI Layer
        ├── GalaxyControls (Leva)
        ├── <AvatarControls /> ◄────────────── NEW (ADD THIS)
        ├── KeyboardHelp
        └── ConstellationInfoPanel


┌──────────────────────────────────────────────────────────────────────────────┐
│                          CONFIGURATION CASCADE                               │
└──────────────────────────────────────────────────────────────────────────────┘

    avatarConfig.ts (Defaults)
           │
           │  const AVATAR_CONFIG = {
           │    WALK_SPEED: 0.15,
           │    RUN_SPEED: 0.3,
           │    ...
           │  }
           │
           ▼
    avatarStore.ts (Initial State)
           │
           │  const initialState = {
           │    moveSpeed: AVATAR_CONFIG.WALK_SPEED,
           │    sprintSpeed: AVATAR_CONFIG.RUN_SPEED,
           │    ...
           │  }
           │
           ▼
    AvatarControls.tsx (Leva UI)
           │
           │  useControls({
           │    moveSpeed: { value: store.moveSpeed, ... }
           │    ...
           │  })
           │
           ▼
    User Adjusts in Real-Time
           │
           │  User drags slider in Leva panel
           │
           ▼
    Store Updates
           │
           │  avatarStore.moveSpeed = 0.25
           │
           ▼
    Movement Recalculates
           │
           │  velocity = direction * store.moveSpeed
           │
           ▼
    Avatar Moves Faster!


┌──────────────────────────────────────────────────────────────────────────────┐
│                      KEYBOARD SHORTCUTS REFERENCE                            │
└──────────────────────────────────────────────────────────────────────────────┘

    ╔═══════════╦══════════════════════════════════════════════════════════╗
    ║    Key    ║                    Action                                ║
    ╠═══════════╬══════════════════════════════════════════════════════════╣
    ║     G     ║  Toggle Game Mode (Enable/Disable avatar controls)      ║
    ║     C     ║  Cycle Camera Mode (Orbit → Third-Person → First-Person)║
    ║  ↑ / W    ║  Move Forward                                            ║
    ║  ↓ / S    ║  Move Backward                                           ║
    ║  ← / A    ║  Rotate Left                                             ║
    ║  → / D    ║  Rotate Right                                            ║
    ║   Space   ║  Jump                                                    ║
    ║   Shift   ║  Sprint (hold while moving)                              ║
    ║     E     ║  Interact (future feature)                               ║
    ╚═══════════╩══════════════════════════════════════════════════════════╝


┌──────────────────────────────────────────────────────────────────────────────┐
│                          STATE TRANSITION DIAGRAM                            │
└──────────────────────────────────────────────────────────────────────────────┘

    Animation States:

         ┌──────┐
         │ IDLE │◄────────────────────────┐
         └──┬───┘                          │
            │                              │
       Arrow key pressed            No input & grounded
            │                              │
            ▼                              │
       ┌─────────┐                    ┌────┴────┐
       │ WALKING │───(Shift pressed)──▶ RUNNING │
       └────┬────┘◄──(Shift released)─└────┬────┘
            │                              │
       Spacebar                        Spacebar
            │                              │
            ▼                              ▼
       ┌─────────┐                    ┌─────────┐
       │ JUMPING │                    │ JUMPING │
       └────┬────┘                    └────┬────┘
            │                              │
     Velocity < 0                    Velocity < 0
            │                              │
            ▼                              ▼
       ┌─────────┐                    ┌─────────┐
       │ FALLING │                    │ FALLING │
       └────┬────┘                    └────┬────┘
            │                              │
       Hit ground                      Hit ground
            │                              │
            └──────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                         FILE DEPENDENCY GRAPH                                │
└──────────────────────────────────────────────────────────────────────────────┘

    avatarConfig.ts ◄─────────────────────┐
         │                                 │
         ├──────────────────┐              │
         │                  │              │
         ▼                  ▼              │
    avatarStore.ts    useAvatarMovement   │
         │                  │              │
         │                  │              │
         ├──────────┬───────┴───┐          │
         │          │           │          │
         ▼          ▼           ▼          │
    useCameraFollow  Avatar.tsx   AvatarControls.tsx
         │          │           │          │
         └──────────┴───────────┴──────────┘
                    │
                    ▼
            AvatarController.tsx
                    │
                    ▼
            HybridScene.tsx


═══════════════════════════════════════════════════════════════════════════════
                             QUICK START CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

  1. ☐ Add imports to HybridScene.tsx
        • import { AvatarControls } from "../ui/AvatarControls"
        • import { AvatarController } from "../avatar/AvatarController"

  2. ☐ Add <AvatarController /> to scene (after AccurateSolarSystem)

  3. ☐ Add <AvatarControls /> to UI layer (after GalaxyControls)

  4. ☐ Save file and check for compilation errors

  5. ☐ Start dev server: npm run dev

  6. ☐ Open browser, press G to enable game mode

  7. ☐ Test movement with arrow keys

  8. ☐ Test camera modes with C key

  9. ☐ Adjust settings in Leva panel

 10. ☐ Enjoy your game-like galaxy explorer!

═══════════════════════════════════════════════════════════════════════════════
